[package]
name = "jsonplaceholder-component"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.26.0"

# WASI Preview 2 HTTP bindings
wasi = "0.14.0"